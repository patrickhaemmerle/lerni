<div class="panel panel-default">
  <div class="panel-heading">Panel heading</div>
  <table class="table">
    <% @cards.each do |card| %>
        <tr>
            <td><%= card[:front] %></td>
            <td><%= card[:back] %></td>
        </tr>
    <% end %>
  </table>
</div>